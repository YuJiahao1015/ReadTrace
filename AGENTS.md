# 项目协作规则

- 始终使用简体中文回复。
- 每次验证完一个功能后，必须及时提交一次 Git commit，避免已验证状态和后续改动混在一起。
- 每次完成一个新功能后，必须在最终回复中输出测试重点，方便用户按场景验证。
- 每次开始修复一个问题前，必须先提交一次当前状态；随后围绕该问题持续修复，直到用户反馈该问题已经修成功。

## 分支与发布规则

- 正式发布线用于沉淀稳定功能，例如 `release/v1.0.5`。
- 设备探测测试线用于汉王、掌阅、海信、老设备等兼容探测，例如 `test/device-probe`。
- 新增稳定功能时，优先从当前正式发布线拉出功能分支开发；验证通过后合回正式发布线。
- 正式发布线的新功能需要同步到设备探测测试线时，只允许从正式发布线合并到测试线。
- 不要把设备探测测试线直接合回正式发布线，避免把未验证的 Provider、SQLite、壁纸设置、设备探测等实验代码带入正式版。
- 测试线中某个兼容能力验证稳定后，只能通过 cherry-pick 或等价的最小改动单独挑回正式发布线。
